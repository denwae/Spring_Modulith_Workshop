spring:
  application:
    name: Workshop Spring Modulith
  datasource:
    url: jdbc:h2:mem:modulith
    username: sa
    password: password
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
